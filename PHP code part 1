<!doctype html>
<html>
    <head>
        <title>PHP test and Final Project Directions</title>
    </head>
    <body>
    <?php
        echo $_SERVER[HTTP_USER_AGENT];
    ?>
    <?php
        echo $_SERVER[DAILY_CHORE];
    ?>
    <?php
        $welcome = "Let's get started with PHP!";
        echo $welcome;
    ?>
    <?php
        for ($users = 1; $users <= 10; $users++) {
            if ($users <= 9) {
                echo $users . ", ";
            } else {
                echo $users . "!";
            }
        }; ?>
        
    <?php
        for ($name = 1; $name <= 10; $name++) {
            if ($name <= 9) {
                echo $name . ", ";
            } else {
                echo $name . "!";
            }
        }; ?>
        
    <?php
        
    Hi <?php echo htmlspecialchars($_POST['name']); ?>.
    You are <?php echo (int)$_POST['age']; ?> years old.
    You have <?php echo (int)$_POST['number of daily chores']; ?> to complete.
    <?php
        echo "You have completed ten tasks."
        echo "You have five more to finish your entire list."
    ?>
    <?php
        if user list of daily tasks = 10
            echo "Please complete half or more."
    ?>
    <?php
        if users completed tasks = n
            return n += 2 points
        elseif users completed tasks < n
            return 0;
        else
            return n -= 1
    ?>
    <?php
        n = 5
        x = 1
        y = x + 1
    ?>
            
    </body>
</html>
